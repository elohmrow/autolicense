# must be unique in a given SonarQube instance
sonar.projectKey=magnolia-module-autolicense
# this is the name displayed in the SonarQube UI
sonar.projectName=Magnolia module auto-license
sonar.projectVersion=1.0
 
# Since SonarQube 4.2, this property is optional if sonar.modules is set.
# If not set, SonarQube starts looking for source code from the directory containing
# the sonar-project.properties file.
sonar.sources=autolicense/src/main
sonar.tests=autolicense/src/test
 
# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
sonar.binaries=autolicense/target/classes
sonar.java.binaries=autolicense/target/classes
 
# Junit reports
sonar.junit.reportsPath=autolicense/target/surefire-reports
sonar.junit.reportPaths=autolicense/target/surefire-reports
sonar.surefire.reportPaths=autolicense/target/surefire-reports
 
# Jacoco reports
sonar.java.coveragePlugin=jacoco
sonar.jacoco.reportMissing.force.zero=true
sonar.jacoco.reportPath=autolicense/target/jacoco.exec
sonar.jacoco.reportPaths=autolicense/target/jacoco.exec
 
# Debug
sonar.verbose=true
